import React from "react";
import ReactDOM from "react-dom/client";
import AitechLabCRM from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AitechLabCRM />
  </React.StrictMode>
);
